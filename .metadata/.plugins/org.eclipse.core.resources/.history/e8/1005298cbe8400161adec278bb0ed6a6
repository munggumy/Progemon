package logic.terrain;

import java.util.ArrayList;

import logic.character.Pokemon;
import logic.terrain.FightTerrain.TerrainType;

public class FightMap {
	public static final int SIZE_X = 8;
	public static final int SIZE_Y = 6;
	private static FightTerrain[][] map = new FightTerrain[6][8];
	
	

	public static final FightTerrain[][] getMap() {
		return map;
	}

	public static final void setMap(FightTerrain[][] map) {
		FightMap.map = map;
	}
	
	public ArrayList<FightTerrain> findMovableBlockAround(int range, FightTerrain ft, ArrayList<FightTerrain> fts){
		if(range >= 0){
			int x = ft.getX();
			int y = ft.getY();
			fts.add(map[x][y]);
			if(x < SIZE_X){
				findMovableBlockAround(range - 1, map[x + 1][y], fts);
			}
			if(x > 0){
				findMovableBlockAround(range - 1, map[x - 1][y], fts);
			}
			if(y < SIZE_Y){
				findMovableBlockAround(range - 1, map[x][y + 1], fts);
			}
			if(y < 0){
				findMovableBlockAround(range - 1, map[x][y - 1], fts);
			}
		}
		return fts;
	}
}
